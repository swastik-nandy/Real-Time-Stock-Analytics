app = "fetcher-service"  # Replace with your Fly app name
primary_region = "sin"

[build]
dockerfile = "./Dockerfile"

[[services]]
internal_port = 8080
protocol = "tcp"

  [[services.ports]]
  port = 8080
  handlers = ["http"]
